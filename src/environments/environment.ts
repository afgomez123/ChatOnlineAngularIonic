// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

  export var firebaseConfig = {
    apiKey: "AIzaSyAQtCNH3bISa05xObTOsGOqSRVVAc9yjN0",
    authDomain: "chatonline-fcdd5.firebaseapp.com",
    databaseURL: "https://chatonline-fcdd5.firebaseio.com",
    projectId: "chatonline-fcdd5",
    storageBucket: "chatonline-fcdd5.appspot.com",
    messagingSenderId: "920971430490",
    appId: "1:920971430490:web:5e3cf5ebb9d251aa"
  };
  // Initialize Firebase
  // this.firebase.initializeApp(firebaseConfig);

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
